public int reverseNumber(int number) {
        String num = "";
        while(number!=0) {
            num+=number%10;
            number = number/10;
        }
        return Integer.parseInt(num);
    }
